apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "library-chart.fullname" . }}-agents
  labels:
    {{- include "library-chart.labels" . | nindent 4 }}
data:
  AGENTS.md: |
{{ .Files.Get "files/AGENTS.md" | indent 4 }}
